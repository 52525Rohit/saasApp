{
  "name": "saas-app",
  "private": true,
  "version": "1.0.0",
  "description": "Production-ready SaaS application with React, Node.js, Express, PostgreSQL, Prisma, JWT/OAuth auth, RBAC, and Stripe.",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
